name: Deploy CI Monitor Pages

# Publishes the CI reliability policy dashboard and the latest committed monitor data.
on:
  workflow_run:
    workflows: ["Monitor vLLM-Ascend CI"]
    types: [completed]
  push:
    branches: ["main"]
    paths:
      - "index.html"
      - "data/history.json"
      - "data/tests.json"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest monitor data
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Build static site artifact
        run: |
          rm -rf _site
          mkdir -p _site/data
          cp index.html _site/index.html
          cp data/history.json data/tests.json _site/data/
          touch _site/.nojekyll

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
